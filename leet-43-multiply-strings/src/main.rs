use leet_43_multiply_strings::BigInt;

fn main() {
    let bigint: BigInt = "123456789012345678901234567980000888".parse().unwrap();
    println!("{} == {:?}", bigint, bigint);
    println!("{}", 65u8 as char);
}
